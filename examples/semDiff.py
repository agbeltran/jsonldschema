# In order to compare two set of schemas, you can use different classes depending
# on whether you network is resolved or not


# First case scenario, your networks are already resolved
def compare_resolved_network(networks):
    print(networks)


compare_resolved_network("XXX")
